const Akinator = require("./Akinator");
const { regions, themes } = require("./constants/Config");
const { AkinatorAnswer } = require("./types/Aki");

module.exports = {
  Akinator,
  regions,
  themes,
  AkinatorAnswer
};